PORT=3000
HOST=0.0.0.0
# Set this when a reverse proxy exposes the app below a URL prefix.
BASE_PATH=
# Set true only when the service runs behind a trusted reverse proxy.
TRUST_PROXY=false
ENABLE_TRY_IT_OUT=false
OPENAPI_FILE=openapi/openapi.yaml
